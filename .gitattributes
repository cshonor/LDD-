# 统一行尾，避免 Windows ↔ Linux 来回传代码时踩坑。
#
# 背景：内核源码和 Makefile 在 Linux 上编译，行尾必须是 LF。
# 如果 Windows 检出时被 core.autocrlf 转成 CRLF，再把 Makefile 上传到 Linux，
# 每行末尾会多一个 \r，make 会把它当成命令的一部分，报类似：
#   *** missing separator. Stop.  或  '/bin/sh: line 1: $'\r': command not found'
# 统一 eol=lf 后，无论在哪台机器上检出，工作副本都是 LF。
* text=auto eol=lf

# .ko 是 ELF 二进制，绝不能被当文本做行尾转换
*.ko binary
